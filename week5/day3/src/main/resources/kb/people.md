# Участники и роли

## Данил

- Telegram: `@aldtoll`.
- Основная профессия: Android-разработчик (Kotlin, Jetpack Compose).
- Возраст: 33.
- Город: Новосибирск (часовой пояс UTC+7).
- В челлендже: единственный «протагонист» — этот репозиторий и все дни принадлежат ему.
- Ролевой стек челленджа: Kotlin, DeepSeek, OpenRouter, MCP, RAG.

## Референсные участники (эталоны решений)

Когда своё решение уже написано, полезно посмотреть, как задачу решил кто-то ещё:

- **uncolorrboy** — эталонный участник, у него есть репозиторий с задачами `task_4.4` (композиция MCP-инструментов), `task_4.5` (оркестрация с serverId-map и function-calling).
- **Александра Орлова** — два MCP-сервера, реестр инструментов с namespace `serverName+toolName`.
- **mobdev778** — репо `aiadventchallenge8` с образцом Streamable HTTP-клиента (использован как ориентир для day16).
- **StrixG** — репо `mcp-cli-kotlin`, вариант stdio-клиента (альтернатива Streamable HTTP).

Их код читаем только после того, как своё решение написано и запущено — чтобы не подсматривать раньше времени.

## Куратор

Курирует поток лектор, ведёт таблицу заданий в Google Sheets. Наши комментарии с ссылками на видео и обсуждения других участников тянет `~/tools/sheets-reader/` (OAuth-токен, обновляется вручную).
